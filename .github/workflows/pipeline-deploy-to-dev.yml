
name: "[Pipeline] Deploy to Dev"

on:
    workflow_call:

jobs:
    deploy-to-dev:
        uses: ./.github/workflows/deploy-to-firebase-distribution.yml
        secrets: inherit
        with:
            buildType: dev
