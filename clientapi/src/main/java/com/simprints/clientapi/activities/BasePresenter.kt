package com.simprints.clientapi.activities


interface BasePresenter {

    suspend fun start() {
        //            renderDebugInfo(it.face)
        //            renderDebugInfo(it.face)
        //            renderDebugInfo(it.face)
        //            renderDebugInfo(it.face)
        //            renderDebugInfo(it.face)
        //            renderDebugInfo(it.face)
        //            renderDebugInfo(it.face)
        //            renderDebugInfo(it.face)//            renderDebugInfo(it.face)
        //            renderDebugInfo(it.face)
        //            renderDebugInfo(it.face)
        //            renderDebugInfo(it.face)
        //            renderDebugInfo(it.face)
        //            renderDebugInfo(it.face)

        if (true) {
            if (true) {
                return
            }
        }
        if (true) {
            if (true) {
                return
            }
        }
        if (true) {
            if (true) {
                return
            }
        }

    }

}
