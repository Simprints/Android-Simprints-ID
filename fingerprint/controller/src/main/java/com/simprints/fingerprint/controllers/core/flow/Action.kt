package com.simprints.fingerprint.controllers.core.flow

/**
 * This enum class represents an action to be handled by the fingerprint module
 */
enum class Action {
    ENROL,
    IDENTIFY,
    VERIFY
}
