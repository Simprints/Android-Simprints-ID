package com.simprints.fingerprint.controllers.core.locationprovider

import com.simprints.fingerprint.tools.utils.LocationProvider

class FingerprintLocationProviderImpl(private val locationProvider: LocationProvider): FingerprintLocationProvider {

}
