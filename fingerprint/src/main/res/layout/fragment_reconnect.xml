<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/connectScannerLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/translucent"
    android:orientation="vertical"
    tools:context="com.simprints.fingerprint.activities.connect.ConnectScannerActivity">

    <View
        android:id="@+id/reconnect_box"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:background="@color/simprints_white"
        app:layout_constraintStart_toStartOf="@id/reconnect_progress_bar"
        app:layout_constraintEnd_toEndOf="@id/reconnect_message_text_view"
        app:layout_constraintTop_toTopOf="@id/reconnect_progress_bar"
        app:layout_constraintBottom_toBottomOf="@id/reconnect_progress_bar"/>

    <ProgressBar
        android:id="@+id/reconnect_progress_bar"
        style="?android:attr/progressBarStyle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:padding="16dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toStartOf="@id/reconnect_message_text_view"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintHorizontal_chainStyle="packed"/>

    <TextView
        android:id="@+id/reconnect_message_text_view"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:paddingEnd="16dp"
        android:paddingStart="0dp"
        android:text="@string/reconnecting_message"
        app:layout_constraintStart_toEndOf="@id/reconnect_progress_bar"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"/>

</androidx.constraintlayout.widget.ConstraintLayout>
