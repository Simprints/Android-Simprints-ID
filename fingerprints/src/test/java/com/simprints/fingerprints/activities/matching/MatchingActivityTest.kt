package com.simprints.fingerprints.activities.matching

import org.junit.Assert.assertEquals
import org.junit.Test

class MatchingActivityTest {

    @Test
    fun sanityCheck() {
        assertEquals(4, 2 + 2)
    }
}
