package com.simprints.id.activities.dashboard.viewModels

enum class DashboardCardType {
    PROJECT_INFO,
    SYNC_DB,
    LOCAL_DB,
    REMOTE_DB,
    LAST_SCANNER,
    CURRENT_USER,
    LAST_IDENTIFICATION,
    LAST_VERIFICATION,
    LAST_ENROL
}
