package com.simprints.id.data.analytics.crashes

enum class CrashReportTags {
    LOGIN,
    SCANNER_SETUP,
    SYNC,
    SESSION,
    FINGER_CAPTURE,
    MATCHING,
    SETTINGS,
    LONG_CONSENT,
    ALERT,
    REFUSAL,
    SAFE_EXCEPTION
}
