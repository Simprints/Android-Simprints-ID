package com.simprints.id.data.analytics.crashreport

enum class CrashReportTag {
    LOGIN,
    SCANNER_SETUP,
    SYNC,
    SESSION,
    FINGER_CAPTURE,
    MATCHING,
    SETTINGS,
    LONG_CONSENT,
    ALERT,
    REFUSAL,
    SAFE_EXCEPTION
}
