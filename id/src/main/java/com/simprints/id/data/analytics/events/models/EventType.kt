package com.simprints.id.data.analytics.events.models

enum class EventType {
    CALLOUT,
    LOGIN,
    CALLBACK,
    ARTIFICIAL_TERMINATION,
    AUTHENTICATION,
    CONSENT,
    ENROLLMENT,
    FINGERPRINT_CAPTURE
}
