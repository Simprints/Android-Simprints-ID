package com.simprints.id.data.analytics.events.models

enum class EventType {
    CALLOUT,
    LOGIN,
    CALLBACK,
    ARTIFICIAL_TERMINATION,
    AUTHENTICATION,
    CONSENT,
    ENROLLMENT,
    FINGERPRINT_CAPTURE,
    ONE_TO_ONE_MATCH,
    ONE_TO_MANY_MATCH,
    PERSON_CREATION
}
