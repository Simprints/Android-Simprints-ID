<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true">

    <com.google.android.material.appbar.AppBarLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:theme="@style/AppTheme.AppBarOverlay">

        <androidx.appcompat.widget.Toolbar
            android:id="@+id/dashboardToolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:background="?attr/colorPrimary"
            app:popupTheme="@style/AppTheme.PopupOverlay" />

    </com.google.android.material.appbar.AppBarLayout>

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginTop="?attr/actionBarSize"
        android:background="?attr/colorPrimary">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <LinearLayout
                android:id="@+id/syncCards"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical">

                <include
                    android:id="@+id/activity_dashboard_card_sync_default"
                    layout="@layout/activity_dashboard_card_sync_default" />

                <include
                    android:id="@+id/activity_dashboard_card_sync_failed"
                    layout="@layout/activity_dashboard_card_sync_failed" />

                <include
                    android:id="@+id/activity_dashboard_card_sync_try_again"
                    layout="@layout/activity_dashboard_card_sync_try_again" />

                <include
                    android:id="@+id/activity_dashboard_card_sync_modules"
                    layout="@layout/activity_dashboard_card_sync_modules" />

                <include
                    android:id="@+id/activity_dashboard_card_sync_settings"
                    layout="@layout/activity_dashboard_card_sync_settings" />

                <!-- State: Progress, Connecting, Complete !-->
                <include
                    android:id="@+id/activity_dashboard_card_sync_progress"
                    layout="@layout/activity_dashboard_card_sync_progress" />
            </LinearLayout>

        </LinearLayout>
    </ScrollView>
</androidx.coordinatorlayout.widget.CoordinatorLayout>
