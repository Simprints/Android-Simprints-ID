<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:title="@string/help_and_support_menu">
        <menu>
            <item
                android:id="@+id/nav_help"
                android:icon="@drawable/ic_menu_help"
                android:title="@string/help_menu" />
            <item
                android:id="@+id/privacy"
                android:icon="@drawable/ic_menu_privacy"
                android:title="@string/privacy_and_consent" />

        </menu>
    </item>

    <item android:title="@string/configuration_menu">
        <menu>
            <item
                android:id="@+id/nav_settings"
                android:icon="@drawable/ic_menu_settings"
                android:title="@string/nav_settings" />
            <item
                android:id="@+id/nav_about"
                android:icon="@drawable/ic_menu_about"
                android:title="@string/about_menu" />
        </menu>
    </item>

    <item android:title="@string/account_menu">
        <menu>
            <item
                android:id="@+id/logout"
                android:icon="@drawable/ic_logout"
                android:title="@string/nav_logout" />
        </menu>
    </item>
</menu>
