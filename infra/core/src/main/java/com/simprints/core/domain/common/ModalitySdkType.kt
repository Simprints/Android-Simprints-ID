package com.simprints.core.domain.common

import com.simprints.core.domain.step.StepParams

/**
 * Marker interface for for enums that list SDK types in each modality.
 */
interface ModalitySdkType : StepParams
