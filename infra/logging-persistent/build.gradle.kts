plugins {
    id("simprints.android.library")
    id("simprints.library.hilt")
    id("simprints.library.room")
    id("simprints.testing.unit")
}

android {
    namespace = "com.simprints.logging.persistent"
}

dependencies {

}
